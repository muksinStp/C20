<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" type="text/css" />
    <title><%= title %></title> <!-- Menggunakan variabel EJS untuk title -->
</head>
<body class="body-edit">
    <div class="container-edit">
        <!-- Judul -->
        <div class="title">
            <h1>Updating Data</h1>
        </div>
        <!-- Form Edit -->
        <div class="form-edit">
            <form method="POST">
                <div class="form-edit-group">
                    <div class="form-group">
                        <label class="form-label">Name</label>
                        <input type="text" name="name" value="<%= user.name %>" required />
                    </div>
                    <div class="form-group">
                        <label class="form-label">Height</label>
                        <input type="number" name="height" value="<%= user.height %>" required />
                    </div>
                    <div class="form-group">
                        <label class="form-label">Weight</label>
                        <input type="number" step="0.01" name="weight" value="<%= user.weight %>" required />
                    </div>

                    <div class="form-group">
                        <label class="form-label">Birthdate</label>
                        <input type="date" name="birthdate" value="<%= user.birthdate %>" required />
                    </div>
                    <div class="form-group">
                        <label class="form-label">Married</label>
                        <div class="radio-group" style="display: flex; flex-direction: column; gap: 0;">
                            <label>
                                <input type="radio" name="married" value="false" <%= user.married ===0 ? "checked" : "" %> /> Not Yet
                            </label>
                            <label>
                                <input type="radio" name="married" value="true" <%= user.married ===1 ? "checked" : "" %> /> Yes
                            </label>
                        </div>
                    </div>
                </div>
                <div class="footer-edit">
                    <button type="submit" class="btn-save">Save</button>
                    <button type="button" class="btn-back"><a href="/">Back</a></button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
