<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" type="text/css" />
    <title><%= title %></title> <!-- Menggunakan variabel EJS untuk title -->
</head>
<body class="body-add">
    <div class="container-add">
        <div class="title">
            <h1>Adding Data</h1>
        </div>
        <div class="form-add">
            <form action="/add" method="POST">
                <div class="form-add-group">
                    <div class="form-group">
                        <label class="form-label">Name</label>
                        <input type="text" name="name" placeholder="insert your name" value="<%= user ? user.name : '' %>" required/>
                    </div>
                    <div class="form-group">
                        <label class="form-label">Height</label>
                        <input type="number" name="height" placeholder="insert your height" value="<%= user ? user.height : '' %>" required />
                    </div>
                    <div class="form-group">
                        <label class="form-label">Weight</label>
                        <input type="number" step="0.01" name="weight" placeholder="insert your weight" value="<%= user ? user.weight : '' %>" required/>
                    </div>
                    <div class="form-group">
                        <label class="form-label">Birthdate</label>
                        <input type="date" name="birthdate" value="<%= user ? user.birthdate : '' %>" required/>
                    </div>
                    <div class="form-group">
                        <label class="form-label">Married</label>
                        <div class="radio-group" style="display: flex; flex-direction: column; gap: 0;">
                            <label>
                                <input type="radio" name="married" value="true" <%= user && user.isMarried ? 'checked' : '' %>> Yes </input>
                            </label>
                            <label>
                                <input type="radio" name="married" value="false" <%= user && !user.isMarried ? 'checked' : '' %>> Not Yet </input>
                            </label>
                        </div>
                    </div>
                </div>
                <div class="footer-add">
                    <button type="submit" class="btn-add">Save</button>
                    <button type="button" class="btn-cancel"><a href="/">Cancel</a></button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
